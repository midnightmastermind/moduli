# Handoff — 2026-06-18 → next session

## TL;DR
On branch **`feat/line-level-wrap-morph`** (pushed to origin). The **line-level wrap-morph feature is built, committed, pushed, building clean, 1116 tests pass.** ONE bug remains — dragging a wrapped image to a new line doesn't re-morph — and it's a **drop-routing** problem (the wrong editor catches the drop). A focused spec exists; do that next.

## Branch / state
- Branch: `feat/line-level-wrap-morph` (origin has it; NOT merged to master).
- `npm run build:client` → clean. `npm run test --silent` → **60 files / 1116 tests pass**.
- Latest commit: `72e2f948` (diagnostic + routing-fix spec). Feature commits: `14416bcf` (wrapAnchor helper+tests), `b1d5e5a6` (anchorOffset attr), `068fee2c` (detectSideHost hoist + side-everywhere + anchorOffset), `4da65484` (per-line drop highlight), `a7efb66d` (docs), plus `fc1f2652` (session UI/wrap polish) + dist rebuilds.

## What SHIPPED this session
1. **Line-level wrap morph** — a wrap neighbor (image) can anchor at ANY visual line, not just block boundaries. New pure helper `client/src/docs/wrapAnchor.js` (`sideFromFrac`, `anchorOffsetForDrop`; tested). New `anchorOffset` (px) attr on the `wrapGroup` node → drives `--wrap-mt` in `WrapGroupNode.measure` (legacy `anchorIndex` kept as fallback). `Editor.jsx detectSideHost` hoisted to component scope, now picks a side EVERYWHERE (removed the dead "middle third" that returned null) and returns `anchorOffset`. New per-line, side-aware `.wrap-drop-line` highlight during drag.
2. **Wrap image-occurrence polish** (`index.css`): framed card (border + brighter-teal bg distinct from parent), image flush-top + fills, caption padded (not jammed), right-edge outer margin (no overlap), seam extended by `BOTTOM_GAP` so the bottom-bar's top border ("the 3rd border going clockwise from the L's top") has margin above the image. The CURRENT state is: bordered + brighter-teal bg is INTENTIONAL (user asked ~12×).
3. **Other UI**: instance-row handle/label/fields vertically centered (single-line) while handle stays top-left for tall bodies; doc-container header handle/label aligned; container label nudged up 1px; **filter ↔ add buttons swapped** in container + page headers; image caption color brightened; mini-textblock chips render identically in/out of a wrap (`display:inline`), use padding (not margin) as spacing, negative margin to eat adjacent spaces; doc `word-spacing:-1px`; **folder PreviewNode labels now use AutoMarquee**; `DragProvider` bails on any drop over a `.doc-editor` (was double-handling).

## THE OPEN BUG → spec to execute next
**`docs/superpowers/specs/2026-06-18-wrap-drop-routing-fix.md`** — READ IT FIRST.
- Symptom: drag a wrapped image top→middle; it does NOT move (cross-doc moves instead of re-morphing).
- Root cause (proven by the diagnostic, NOT a detectSideHost logic bug): the WRONG editor handles the drop. Live log shows `detectSideHost null — depth<1 {pos:0}` (posAtCoords found nothing in that editor) + `grouped-member? null` (the image's wrapGroup isn't in that editor's doc). The stray editor is persistent: `ed=6c94f267` (appears across many drops, always `insertPos 0`, source never found).
- **Do Part A step 1 FIRST (~10 min):** log what `6c94f267` IS (its `occurrence?.id`, module role/kind via the refs, `mode`, ancestor classes) at onDrop entry. The diagnostic `[detectSideHost] null — <reason>` logging is ALREADY live in the build. That single line unblocks the fix.
- Part A: close the registration gap so the PAGE editor (whose `findGroupMember` finds the occ) owns the drop. Part B: make the re-morph occurrence-driven via a new `wrapMemberRegistry` (mirror `embedDeleteRegistry`) so it works regardless of which editor caught the event. Full detail + acceptance criteria in the spec.

## Also parked (not started)
- **QuickAddMenu → DrilldownPicker redesign** — brainstormed + decisions captured (6 type tiles: Container/Instance/Textblock/Artifact/Page/Panel; browse-or-create per type; ALL types everywhere; **Board NOT List** — list=board, see memory `feedback_list_is_board`; keep field-picking + templates). No spec/plan written yet; resume from those decisions.
- Backslash `\` escape artifacts in imported doc text → markdown importer escape-stripping (future imports only).

## Watch-outs
- Drag/drop is NOT unit-testable (DOM + native DnD) — verify in the running app.
- The wrap layout has many interacting CSS rules in `index.css` under `.wrap-group--on`; mis-modeled repeatedly under heavy context. Build an accurate DOM picture from a fresh screenshot before tweaking.
- User runs 3 parallel Claude accounts (memory `feedback_multi_account_coordination`) — check for same-file conflicts.
