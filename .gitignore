node_modules/
.env
server/.env
codex/
notes_codex_annotated/

# Git worktrees (superpowers isolated workspaces)
.worktrees/

# build output (built on deploy, not tracked)
client/dist/

# local headless probe scripts + user screenshots (kept local, never deployed)
# UNANCHORED on purpose: a probe heredoc'd while the shell sits in client/
# lands at client/_*.mjs, and deploy.sh's `git add -A` swept two of those
# into deploy commits (2026-07-12 + 2026-07-13) before this was widened.
_*.mjs
_*.png
screenshots/

# Grid backups (server/scripts/backupGrid.js). Live user data — never committed,
# and never swept into a deploy commit by deploy.sh's `git add -A`.
backups/

# throwaway probe suites (deploy.sh runs `git add -A`)
client/src/__tests__/_*.test.js
