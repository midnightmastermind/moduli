we need a view called preview that in the case of an artifact, is a view of a thumbnail of the artifact and the name or label. this should be a view on an artifact module for now but it will be used for other stuff soon. it should be called preview view. so module type, artifact, but the view is preview. then that view allows us to drilldown into the artifact and the option to embed wherever it is currently