// modules/Instance.jsx — re-export stub. Implementation merged into ModuleInstance.jsx.
// Default export is the memoized inner row (InstanceInner) — same as original Instance.jsx default.
export { MemoInstanceInner as default } from "./ModuleInstance.jsx";
