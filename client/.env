# Leave VITE_SERVER_URL unset so the SPA connects back to the same origin
# it was loaded from (works for localhost, WSL IP, LAN IP, prod). Set this
# only when the API lives on a different host than the static assets.
# VITE_SERVER_URL=http://localhost:5000
